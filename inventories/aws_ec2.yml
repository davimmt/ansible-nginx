---
plugin: aws_ec2

regions:
  - sa-east-1

filters:
  tag:Environment:
    - PRD

keyed_groups:
  - key: tags.Name
    prefix: ''
    separator: ''

hostnames:
  - network-interface.addresses.private-ip-address

compose:
  ansible_host: private_ip_addess